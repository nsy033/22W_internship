import React, { useRef, useEffect, useState } from 'react';
import { select } from 'd3';

function StackedBar() {
    return (
        <React.Fragment>
            <h1>Show horizontal stacked bar</h1>
        </React.Fragment>
    );
}

export default StackedBar;